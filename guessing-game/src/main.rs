#[allow(dead_code)]
// use std::io;
use rand::Rng;
use std::cmp::Ordering;
use dialoguer::{theme::ColorfulTheme, Input};

mod validate;
mod guessing_games;

fn main() {

    println!("Let's play some guessing games");


}
